# AGENTS.md · OneTake 项目工作守则

> 本文件是本项目对 AI 编码助手的持久约定，每次会话自动生效。
> 事实源优先级：**本文件（工作方式）> `prd.md`（做什么）> `TODO.md`（做到哪了）**。
> `OneTake项目PRD与技术设计文档.pdf` 为 v1.1 历史存档，不再更新。

## 1. 项目定位

OneTake（一条过）：端到端 AI 视频创作 Agent 工作台 + 其下的小型 AI 基础设施平台。

- 产品面：输入选题，Agent 自动完成「大纲 → 脚本 → 分镜 → 素材生成 → 配音 → 自动剪辑 → 成片」，中间节点可查看、可修改、可重生成（确认式自动化）
- 架构面：所有模型调用经自建模型服务层（OpenAI 兼容内部 API）与任务调度器承载，全链路可观测、数据可沉淀——一个单机版"迷你剪映 AI 基础设施"
- **这个项目存在的根本目的是找工作**（2027 届校招），不是做产品。一切技术决策都要能转化为面试叙事；遇到"更工程化但讲不出故事"和"更简单但故事完整"的分岔时，选后者

## 2. 双 JD 叙事（改动时不可破坏的证据链）

项目同时服务两个目标岗位，同一代码库两种讲法：

- **岗位 A：B 站【主站】AI 创作项目工程师**。证据链：创作管线、LangGraph 编排、Skills/记忆、VLM 质检、B 站真实发布 ≥3 条
- **岗位 B：字节 后端研发工程师（AI 架构）– 剪映 CapCut**。证据链：模型服务化（注册表/灰度/熔断）、任务调度器、可观测性、数据链路、数据驱动决策

两条铁律（两个 JD 共同的结构前提，改动需极其谨慎）：
1. 所有外部模型调用的唯一入口是模型服务层（管线节点不得直接 import 厂商 SDK）
2. 所有异步重负载的唯一入口是任务调度器（jobs 表）

## 3. 环境约定（2026-08-04 实测核实，勿凭记忆假设）

- **FFmpeg**：一律使用 full 版 `/opt/homebrew/opt/ffmpeg-full/bin/ffmpeg`（全局 homebrew ffmpeg 是 lite 版，无 libass，烧不了字幕）。代码中走 `FFMPEG_PATH` 环境变量，默认值即上述路径；ffprobe 同理取同目录
- **TTS**：edge-tts 免费可用但有瞬断（NoAudioReceived），调用层必须重试 ≤3 次；备选火山 TTS（¥1–5/万字符）
- **模型与价格**：以 `serving/pricing.py` 为唯一事实源（2026-08-04 核实版）。关键结论：DeepSeek 用 `deepseek-v4-flash`；VL 质检用 `qwen3-vl-flash`；Seedance 2.0 按 token 计费（贵），草稿档用 1.5 pro 样片模式或可灵 2.5 Turbo（¥0.3/s）；P0 需实测对比后定主力
- **预算**：全项目充值 ≤¥300；网关/服务层日熔断 `DAILY_BUDGET_LIMIT=15`，写代码时这是硬约束不是可选项
- **不引入外部中间件**：不用 Redis/Kafka/Prometheus，持久化一律 SQLite + 本地文件（按规模选型，这是面试答题点）
- **密钥**：一律走 `.env`，真密钥永不入库；`.env.example` 保持同步更新
- **BGM**：只用 `assets/bgm/` 内 Pixabay Music 曲目，来源清单随文件维护
- **B 站发布**：投稿必须勾选「该视频使用人工智能合成技术」创作声明（法规义务）

## 4. 开发节奏

- 按 `TODO.md` 的 checkbox 推进，当前阶段见其顶部「当前阶段」行；完成任务**随手勾掉**并更新「最近更新」，范围变更记入文末「变更日志」
- 每周五晚必须有可运行交付物；功能导向而非技术导向
- 演进纪律：线性版管线是主路径，LangGraph 并行重构、回归验证后转正——不允许"框架先行、功能烂尾"
- 最小可交付底线：P2 端到端 v1 不可让渡；砍 Scope 顺序 P2 > 1 条发布 > P3 > P4 > P5 > P6
- 新代码匹配周边风格；最小改动；不加投机性的"以后可能用到"的抽象
- 验收文化：每个功能完成后跑真实验证（真实 API/真实文件），不许把没跑过的代码说成完成

## 5. Git 约定（重要）

- **每完成一个功能（对应 TODO.md 的一个或多个 checkbox），自觉 `git commit` 并 `git push` 到 remote**，不用等用户提醒
- commit 前自查：无密钥/无大文件（`projects/` 产物已 gitignore）、相关 checkbox 已勾
- commit message 用 Conventional Commits 简体中文版式：`feat: ...` / `fix: ...` / `docs: ...` / `chore: ...` / `refactor: ...`，一行说清做了什么，涉及成本或价格口径变化时正文注明
- 一个 commit 只做一件事；不顺手提交无关改动
- 例外：用户明确说"先别提交"时除外

## 6. 交流风格（求职导向）

这个项目是为找工作存在的，所以在日常交流中：

- **多科普**：涉及概念/技术选型时，用简短段落讲清"它是什么、为什么这样选、业界常见替代方案是什么"——用户在积累面试谈资，不是在听你报流水账
- **预判面试问题**：每完成一个功能模块，主动补一段「这个功能面试可能怎么考」，给出 1–3 个高频问题 + 答题骨架（参考 `prd.md` 附录 C 的风格），并挑值得沉淀的追加到 `prd.md` 附录 C
- **量化意识**：凡能产出数字的地方（成本、命中率、成功率、时延），提醒用户记录，简历要用真实数据
- 科普和预判要简短、嵌在工作汇报之后，不要喧宾夺主；用户说"跳过讲解"时照办

## 7. 关键文件地图

| 文件 | 作用 |
|---|---|
| `prd.md` | v2.0 活文档：需求、架构、双 JD 映射、面试问答（附录 C） |
| `TODO.md` | 任务清单与进度，勾选式推进 |
| `.prd.txt` | v1.1 PDF 的文本提取版，仅供检索 |
| `serving/pricing.py` | 模型单价唯一事实源（建仓后） |
| `.env` / `.env.example` | 密钥（真密钥不入库） |
