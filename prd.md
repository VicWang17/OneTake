# OneTake · 端到端 AI 视频创作 Agent 工作台与小型 AI 基础设施平台

> 产品需求文档（PRD）与技术设计文档 · **v2.0（活文档，以此为准）**
> 编写日期：2026-08-04 · v1.1 见《OneTake项目PRD与技术设计文档.pdf》（历史存档）
> 项目类型：个人独立开发项目（简历项目）
> 目标岗位：
> ① 哔哩哔哩【主站】AI 创作项目工程师（2027 届）
> ② 字节跳动 后端研发工程师（AI 架构）– 剪映 CapCut（2027 届，职位 ID: A83621E）
> 预算上限：¥300（推荐执行档 ¥100–150）

## v2.0 变更说明

相对 v1.1 的三类变更：

1. **新增第二目标岗位（剪映 AI 架构）**：项目从"AI 视频工具"升级为"AI 视频创作 Agent + 其下的小型 AI 基础设施平台"，新增四大基础设施模块：任务调度（Task Scheduler）、模型服务化（Model Serving）、可观测性（Observability）、数据链路（Data Pipeline）。四大模块均为现有管线的自然升级，不引入新的外部依赖与预算。
2. **价格与模型核实结果回填**（2026-08-04 联网 + 本地实测）：Seedance 2.0 改按 token 计费、DeepSeek 模型名更新、百炼免费额度口径修正、本机 ffmpeg 无 libass 等，详见第 3 章。
3. **工期 8 周 → 9 周**：新增 W5/W6 两个基础设施周（全本地、近零成本），最小可交付定义不变。

---

## 1 项目介绍（PRD）

### 1.1 项目背景与动机

项目同时覆盖两个 2027 届校招目标岗位：

**岗位 A：B 站【主站】AI 创作项目工程师。** 对应公司级产品 Updream（一站式 AI 视频创作平台，Agent 助手 + Skills 社区 + 画布工作流 + 多模型聚合）。JD 五条职责：AI Agent 视频创作系统、前沿大模型调研落地、AI 工具链与自动化工作流、敏捷迭代端到端负责、Agent/Tool/Workflow 架构与大模型工程化。

**岗位 B：字节跳动 后端研发工程师（AI 架构）– 剪映 CapCut。** 团队支持剪映/CapCut/醒图/即梦 Dreamina 等影像创作与 GenAI 产品。JD 四条职责：
1. 后端和 AI Agent 方向的系统设计、开发和迭代，支持 AI 策略与模型的**高效部署与服务化**；
2. 构建与优化**数据处理、特征生产**等 AI 工程化链路；
3. 通过**数据分析与挖掘**，为模型训练、**风险感知与治理**提供数据与工程支持；
4. 协同算法、数据与前后端团队，推动技术方案**从实验到上线**的完整落地。

两个 JD 共享同一条证据链：岗位 A 的"创作管线"产生真实的模型调用、任务负载与数据；岗位 B 的"基础设施"正是承载这些负载所需的调度、服务化、观测与数据能力。一个项目、两种讲法，互为因果而非拼凑。

**项目动机**：以独立开发者身份，用 9 周、不超过 ¥300，构建"Updream 的最小可行复刻 + 其下的迷你剪映 AI 基础设施"，并用它实际产出成片完成端到端验证（不公开发布；开发者本身具备丰富视频创作经验，岗位 A 的创作经验加分项由既有经历覆盖）。形成"系统设计能力 + 工程落地能力 + 真实内容产出 + 数据驱动决策"的四重证据链。

### 1.2 项目定位与目标

一句话定位（产品面）：输入一个选题，Agent 自动完成"大纲 → 脚本 → 分镜 → 素材生成 → 配音 → 自动剪辑 → 成片"的完整生产流程；每个中间节点可查看、可修改、可重生成 —— "确认式自动化"。

一句话定位（架构面）：所有模型调用经由自建的模型服务层（OpenAI 兼容内部 API）与任务调度器承载，全链路可观测、数据可沉淀 —— 一个小型"剪映 AI 基础设施"的单机缩影。

项目定名 OneTake（一条过）：取自片场术语"一条就过"。

表 1-1 项目目标（约束即目标）

| 维度 | 目标值 |
|---|---|
| 开发周期 | 9 周，每周一个可运行里程碑（第 3 周末端到端跑通，第 6 周末基础设施成型） |
| 人力 | 1 人独立开发，普通笔记本电脑，无 GPU、无独立服务器 |
| 资金预算 | 推荐档 ≤¥150，硬上限 ¥300 |
| 内容产出 | 用自研工具产出 ≥3 条成片（480p 即可，用于端到端验证与演示；不公开发布） |
| 公开产出 | GitHub 开源仓库（README + 架构图 + 演示视频），可一键复现 |
| 双 JD 覆盖 | 同一代码库同时支撑两个岗位的面试叙事（见 1.7 映射表） |

### 1.3 目标用户与核心场景

首要用户是开发者自己（"工具作者即用户"：自己作为真实用户形成"需求 → 迭代 → 产出"闭环；岗位 A 的创作经验加分项由开发者既有经历覆盖）；次要用户为面试官（通过演示视频与开源仓库体验）。

表 1-2 核心使用场景

| 编号 | 场景 | 描述 |
|---|---|---|
| S1 | 知识科普短视频生产 | 输入选题产出 60–90 秒解说视频，全自动，人工仅在脚本与分镜两处确认 |
| S2 | 风格化批量产出 | 创作方法论封装为 Skill 包，同一 Skill 经任务调度器批量产出多条视频 |
| S3 | 低干预自动化 | 跳过中间确认节点，一键选题到成片，VLM 质检自动拦截低分镜头 |
| S4（新） | 模型灰度与对比 | 通过模型服务层把 10% 流量路由到新模型/新供应商，数据链路产出质量-成本对比报告，据此决定主力模型 —— 演示"从实验到上线" |
| S5（新） | 批量任务运维 | 批量出片时通过 CLI 观察队列深度、成功率、失败重放（死信队列），演示任务调度的运维面 |

### 1.4 功能范围（MoSCoW 分级）

表 1-3 功能分级清单

| 级别 | 功能 |
|---|---|
| Must（必须） | 完整创作管线（选题→脚本→分镜→分镜图→镜头视频→TTS→自动剪辑）；两处人工确认；断点续跑；幂等缓存；成本日志与报表；草稿/成片两档；**任务调度器（队列/重试/死信/限流）；模型服务层（注册表/路由/降级/灰度）** |
| Should（应该） | Skills 技能库（≥2 个）；长期记忆；VLM 质检与自动重生成；发布物料生成；**可观测性（结构化日志/trace/metrics/stats 仪表盘）；数据链路（事件日志/每日聚合/模型对比报告/失败模式挖掘）** |
| Could（可以） | 素材语义搜索；角色一致性增强；简易 Web 界面（含只读观测面板） |
| Won't（明确不做） | 自训练或微调模型；GPU 本地部署生成模型；节点式画布前端；多用户账号体系；移动端；**不引入 Redis/Kafka/Prometheus 等外部中间件（单机 SQLite 优先，理由见 4.6）** |

范围控制原则：自研精力投入编排、Agent、成本工程、**AI 基础设施（调度/服务化/观测/数据）**这些与双 JD 强相关的差异化能力；模型能力一律外购 API；前端能 CLI 就不写页面。

### 1.5 成功指标

表 1-4 可量化验收指标

| 指标 | 目标值 |
|---|---|
| 端到端出片时长（60s 视频，草稿档） | ≤ 30 分钟/条 |
| 单条成本 | P0 用真实 API 重测后回填目标值（v1.1 目标：草稿 ≤¥5、成片 ≤¥12，2026-08 核实后需修正，见 3.6） |
| 项目总支出 | ≤ ¥300（推荐档 ≤¥150） |
| 成片产出 | ≥ 3 条（480p，留存 + 作为 README 演示素材；不公开发布） |
| 缓存命中率 | ≥ 70%，续跑零重复扣费 |
| VLM 质检一次通过率（终稿） | ≥ 80% 镜头免重生成 |
| **任务调度** | 批量任务含重试成功率 ≥ 95%；worker 宕机恢复后无任务丢失/重复扣费；死信可人工重放 |
| **模型服务化** | 管线节点 100% 经由 serving 层 HTTP 调用；灰度路由演示（10% 流量切新供应商） |
| **可观测性** | 单次运行可用 trace_id 串起全部节点与任务；`stats` 一条命令输出成本/成功率/时延/队列状态 |
| **数据链路** | 每日聚合产出模型对比表与 skill 效果表；≥1 次基于运行数据的决策记录（如切换主力视频模型） |
| 开源仓库 README | 30 分钟内可复现跑通；含架构图与演示视频 |

### 1.6 成本预算总览

成本判断不变：管线中唯一显著付费环节是视频生成。但 2026-08 实测发现价格结构变化（Seedance 2.0 按 token 计费后约为 v1.1 口径的 6–8 倍，原定草稿档 1.5 pro 已 Retiring），成本策略相应调整：**草稿档改用 Seedance 2.0 mini/fast 或 480p 档；可灵因 API 采购门槛暂缓接入**（详见 3.6）。

表 1-5 全项目预算（9 周，推荐档）

| 用途 | 估算 |
|---|---|
| 开发调试期（约 30 条草稿级运行，含失败浪费） | ¥100–150 |
| 成片渲染（5–8 条，480p 档） | ¥20–40 |
| 扣减各平台新用户免费额度 | −¥80–120 |
| 基础设施周（W5/W6，全本地） | ≈¥0 |
| 预计实付 | ¥80–150（上限 ¥300） |

预算护栏：网关/服务层设 `DAILY_BUDGET_LIMIT`（默认 ¥15/天）硬熔断；每个平台先耗尽免费额度再充值。

### 1.7 与双 JD 的映射

表 1-6 岗位 A（B 站 AI 创作）职责 → 项目证据

| JD 职责 | 交付物 |
|---|---|
| 1. AI Agent 视频创作系统 | 端到端创作管线 + LangGraph 编排 + 素材资产表 |
| 2. 前沿大模型调研与落地 | 6 类模型真实接入、对比选型文档与成本数据（含 2026-08 价格核实记录） |
| 3. AI 工具链与自动化工作流 | 脚本生成器 + EDL 时间线 + FFmpeg 自动剪辑引擎 |
| 4. 技术验证到产品化，敏捷迭代 | 每周可运行里程碑；自研管线产出 3+ 条成片完成端到端验证（创作经验由开发者既有经历覆盖） |
| 5. Agent/Tool/Workflow 架构 | 模型服务层、Skills、记忆、VLM 质检回路 |
| 加分项：视频平台创作经验 | 由开发者既有创作经历覆盖（本项目不公开发布，仅提供"工具作者即用户"的自用闭环佐证） |

表 1-7 岗位 B（剪映 AI 架构）职责 → 项目证据

| JD 职责 | 交付物 |
|---|---|
| 1. 后端 + AI Agent 系统设计，模型高效部署与服务化 | 模型服务层（OpenAI 兼容内部 API、模型注册表、路由/降级/灰度）+ LangGraph Agent 编排 |
| 2. 数据处理、特征生产等 AI 工程化链路 | 数据链路：事件日志 → 每日聚合 → 模型/Skill 效果特征表；任务调度器承载批量负载 |
| 3. 数据分析与挖掘，支持风险感知与治理 | 模型对比报告、失败模式挖掘、内容审核结果落库与敏感题材管控；VLM 质检数据 |
| 4. 推动技术方案从实验到上线 | 灰度路由（10% 流量实验）→ 数据对比 → 全量切换的完整演示；每周里程碑 |

两岗交叉增强：岗位 A 的"成本工程"与岗位 B 的"可观测性/数据链路"是同一套系统的两个视角；岗位 B 的"服务化"让岗位 A 的"多模型聚合"叙事从 SDK 封装升级为平台能力。

---

## 2 系统总体架构

### 2.1 架构总览

v2.0 采用五层架构 + 一个横切面：

```
┌─────────────────────────────────────────────────────┐
│ 接入层        CLI（main.py / report / stats / jobs）    │
├─────────────────────────────────────────────────────┤
│ 编排与能力层   管线编排器 · 脚本分镜 · 素材生成 · 自动剪辑 │
│               Skills 与记忆 · VLM 质检                 │
├──────────────────┬──────────────────────────────────┤
│ 模型服务层        │ 任务调度层                          │
│ OpenAI 兼容 API  │ 任务队列 · worker 池 · 重试/死信     │
│ 注册表·路由·灰度  │ 优先级 · 按 provider 限流           │
├──────────────────┴──────────────────────────────────┤
│ 基础设施层     SQLite · 文件存储 · FFmpeg              │
└─────────────────────────────────────────────────────┘
   横切：可观测性（结构化日志 / trace_id / metrics / 告警）
          数据链路（事件日志 → 聚合 → 特征表 → 报告）
```

图 2-1 系统总体架构。两条铁律：① 所有外部模型调用的唯一入口是模型服务层（HTTP）；② 所有异步重负载（视频生成、批量出片）的唯一入口是任务调度器。这是成本工程、可观测性与数据链路得以成立的结构前提。

### 2.2 模块划分

表 2-1 模块职责与关键技术

| 模块 | 职责 | 关键技术 |
|---|---|---|
| 管线编排器 | 驱动选题→成片的 DAG/状态机，中断、续跑、单节点重跑 | LangGraph StateGraph + SQLite checkpoint（P0–2 线性函数版） |
| 脚本与分镜 | 大纲/分镜表生成（JSON Schema 约束）、台词时长估算 | DeepSeek API，结构化输出 + 校验重试 |
| 素材生成 | 分镜图批量生成、图生视频任务提交 | Seedream / Seedance / 可灵 API（经 serving 层） |
| 自动剪辑 | EDL 时间线、拼接、字幕烧录、BGM 混音 | FFmpeg filter_complex、SRT/ASS |
| **模型服务层** | 全部外部模型调用的唯一入口：OpenAI 兼容 API、模型注册表、档位/成本/灰度路由、幂等缓存、计费、失败降级、熔断 | FastAPI + YAML Registry + sha256 幂等键 |
| **任务调度器** | 异步任务的队列、调度、并发控制、重试、死信、优先级 | SQLite 任务表 + asyncio worker 池 |
| **可观测性** | 结构化日志、trace 贯穿、metrics 采集与仪表盘、告警 | JSON lines 日志 + SQLite metrics + CLI stats |
| **数据链路** | 事件日志、每日聚合、特征表、分析报告、失败模式挖掘 | SQLite 事件表 + 调度器驱动聚合任务 |
| Skills 系统 | Skill 包定义/注册/选择/加载 | YAML Schema + LLM 选择器 |
| 记忆系统 | 画像/经验记忆的提取、合并、检索、注入 | SQLite + LLM 提取（Mem0 式流程） |
| 质量评测 | 镜头级自动评分与低分重生成回路 | Qwen-VL API（VLM-as-Judge） |

### 2.3 核心数据模型

全部数据落 SQLite + 本地文件目录（素材按 `projects/{id}/` 组织）。核心表：

表 2-2 核心表设计（字段节选）

| 表 | 关键字段 | 说明 |
|---|---|---|
| projects | id, topic, skill_id, status, created_at | 一次创作任务 |
| shots | id, project_id, idx, duration, visual_prompt, narration, status | 分镜，状态机见 2.4 |
| generations | id, idem_key(unique), model, tier, prompt, params, cost, file_path, created_at | 每次生成调用档案，idem_key 幂等 |
| **jobs** | id, type, payload_json, priority, status(pending/running/succeeded/failed/dead), retry_count, max_retries, run_at, worker_id, idem_key, created_at, finished_at | 任务调度器核心表 |
| **events** | id, ts, trace_id, kind(generation/eval/publish/job), ref_id, data_json | 数据链路的事件日志（append-only） |
| **model_perf_daily** | date, model, tier, calls, success_rate, avg_latency, total_cost, avg_quality | 聚合特征表：模型日维度表现 |
| skills | id, name, version, yaml_path, metrics_json | Skill 包注册表与效果数据 |
| memories | id, type(profile/episode), content, confidence, updated_at | 长期记忆条目 |

### 2.4 分镜状态机与管线流程

与 v1.1 一致：分镜（shot）为最小管理单元，状态机为 分镜创建 → 分镜图生成 → 人工确认 → 草稿视频生成 → VLM 质检 → 终稿渲染 → 合成成片；任何阶段失败或打回只回退该分镜，不影响其他分镜已生成资产。

设计要点（不变）："先图后视频"——把构图、风格、角色决策前置到便宜的图片阶段确认，视频阶段只做"让确认过的画面动起来"。

---

## 3 模型推荐与调用方式（2026-08-04 实测核实版）

### 3.1 选型总表

表 3-1 模型选型总表（2026-08-04 官方文档核实）

| 任务 | 推荐模型 | 调用方式 | 单价 | 选择理由 |
|---|---|---|---|---|
| LLM：大纲/脚本/分镜/规划 | `deepseek-v4-flash` | API | 输入 $0.14、输出 $0.28 / 百万 token（缓存命中 $0.0028） | 长文本创作强、价格最低档、OpenAI 兼容；注意高峰时段（9–12/14–18 点）拟 2 倍计费 |
| LLM 备选/降级 | `qwen3.7-flash`（百炼） | API | ¥0.2/0.8 每百万 token；每模型 100 万 token 免费额度（90 天，仅北京） | 免费额度兜底 |
| 文生图：分镜图 | Seedream 4.0（`doubao-seedream-4-0-250828`） | API | ¥0.20/张 | 预算档；5.0 Pro（¥0.30/张）为质量升级项；参考图输入支持 |
| 视频：草稿档 | Seedance 2.0 mini / 2.0 fast（或 2.0 的 480p 档） | API | P0 查实（2.0 标准档 480p ≈¥0.46/s 为上界参考） | 原定 1.5 pro 样片模式已 Retiring（2026-08-04 实测）；可灵因采购门槛暂缓（见 3.5），P0 对比后定草稿主力 |
| 视频：成片档 | Seedance 2.0（`doubao-seedance-2-0-260128`）/ 2.5（`doubao-seedance-2-5-260628`，已上线） | API | 按 token（2.0 720p 文生视频 ≈¥0.99/s） | 账号实测可用；P0 与可灵对比后定成片主力 |
| 配音 TTS | edge-tts | 本地 | ¥0 | 实测可用但有瞬断，调用层必须重试 ≤3 次 |
| TTS 备选 | 火山大模型语音合成 | API | ¥5/万字符（精品长文本 ¥1/万字符） | 一条 60s 配音约 ¥0.1–0.5 |
| 质量评测 | `qwen3-vl-flash`（百炼） | API | ¥0.15/1.5 每百万 token，有独立 100 万免费额度 | VLM-as-Judge 近零成本 |
| 素材向量检索（Could） | BGE / CLIP | 本地 | ¥0 | CPU 可跑 |
| 剪辑合成 | FFmpeg | 本地 | ¥0 | 注意：本机 homebrew 构建无 libass，需换装（见 5.2 P0） |

### 3.2 API 还是本地部署：决策原则

与 v1.1 一致：质量敏感 + 重资产 → 走 API（视频/图像生成、旗舰 LLM）；可被免费方案覆盖 + 高频 → 走本地（TTS、Embedding、剪辑）。本项目全部生成端模型走 API，处理型任务走本地，整机无 GPU 依赖。

### 3.3 调用方式

各模型 SDK 调用示例与 v1.1 一致（DeepSeek OpenAI 兼容、方舟 Ark SDK 同步出图/异步视频任务、edge-tts 命令行、FFmpeg filter_complex），差异在于：**v2.0 起这些调用全部封装在模型服务层适配器内，管线节点一律经 HTTP 调用 serving 层**（见 4.7）。

### 3.4 模型服务层：成本工程与服务化的统一设计

模型服务层是 v1.1"模型网关"的服务化升级。v1.1 的四机制（档位路由、幂等缓存、计费日志、失败降级）全部保留，新增注册表、灰度与熔断，技术设计见 4.7。

### 3.5 免费额度与采购清单（2026-08-04 核实版）

表 3-2 平台注册清单（按优先级）

| 平台 | 免费额度（核实现状） | 用途 |
|---|---|---|
| 火山引擎（方舟） | 每模型 50 万 token 基础额度 + 协作奖励计划（授权数据采集后每日每模型最高返 500 万 token，30 天有效） | Seedream 图 + Seedance 视频主力 |
| DeepSeek | **2026-08-04 实测：赠送余额 ¥0**（"新用户 500 万 token"未兑现），账号充值余额 ¥8.77，按单条 LLM 成本 <¥0.1 测算足够全程 | 全部 LLM 调用主力 |
| 阿里云百炼 | 每模型总 100 万 token（输入+输出合计），90 天，仅华北2（北京） | Qwen LLM 降级 + Qwen-VL 质检 |
| 可灵开放平台 | **暂缓接入（2026-08-04 实测）**：API 无在线充值、需联系客服采购，不适合 ¥300 小额预算；调研已完成，保留适配器位待后续接入 | 视频生成互备（暂缺，见 5.11 风险表） |
| BGM | Pixabay Music（免费商用、无需署名） | 背景音乐，非模型但为发布前提 |

### 3.6 成本策略修正（2026-08 核实后的结论）

1. Seedance 2.0 按 token 计费，文生视频 5s 480p≈¥2.31、720p≈¥4.97，约为 v1.1 口径 6–8 倍；原定草稿档 1.5 pro 样片模式已 Retiring，**草稿档在 seedance 2.0-mini/fast 与 2.0 的 480p 档之间实测选择**。
2. 可灵（720P ¥0.3/s，理论更便宜）因 API 不支持在线充值、需客服采购而**暂缓接入**——单人 ¥300 预算项目不走商务采购流程。**P0 各候选各跑一条 5s 样片实测对比后定主力**，此后由数据链路持续产出对比报告（见 4.9），形成"实验→数据→切换"的完整叙事。可灵作为已调研待接入供应商保留适配器位（其 JWT 鉴权差异——AK/SK 本地签名——已在设计中预留）。
3. 单条成本目标在 P0 实测后回填到表 1-4。

---

## 4 核心模块技术设计

### 4.1 管线编排：从线性脚本到 LangGraph

与 v1.1 一致：P0–2 线性函数版，P3 重构为 LangGraph StateGraph + SQLite checkpointer + 两处 interrupt 人工节点。线性版保留为主路径，LangGraph 并行切换、回归验证后转正——防止"框架先行、功能烂尾"。

### 4.2 脚本与分镜生成

与 v1.1 一致：两阶段生成（大纲 → 分镜表），JSON Schema 校验 + 错误回灌重试 ≤3 次；台词时长超镜头 ±20% 触发改写，仍超则在 EDL 层以音频真实时长为准。

### 4.3 分镜图生成与角色一致性

与 v1.1 一致：角色设定表（文本锚点，零成本）+ 参考图链（首镜确认图作为后续参考图，Seedream 4.5 起最多支持 10 张参考图）+ 人工确认兜底（y/n/r + 修改意见重生成）。选材硬约束：避开人物大场景。

### 4.4 视频生成策略（草稿/成片分层）

两档策略保留，价格口径按 3.6 修正。变化点：**任务提交/轮询/重试不再由素材生成模块自理，统一提交到任务调度器**（4.6）；motion_prompt 由 LLM 基于分镜图与运镜字段生成。

### 4.5 自动剪辑引擎

与 v1.1 一致：EDL（剪辑决策表）与渲染分离；concat 拼接、SRT 硬字幕（样式由 Skill 定义）、BGM 人声闪避（旁白段 BGM 降至 15%）；演示物料（标题 + 封面，用于 README 展示）。

粒度对齐规则：Seedance/可灵按 5s/10s 出片 vs 分镜 4–8s 时长 → EDL 入出点裁剪；声画同步以 TTS 真实时长为准。

### 4.6 任务调度器（Task Scheduler）★ 新增

**定位**：全部异步重负载的唯一入口。视频生成、批量出片、聚合任务一律入队，由 worker 池消费。

表 4-1 任务调度器设计

| 机制 | 实现 |
|---|---|
| 任务表 | `jobs` 表（见 2.3），状态机：pending → running → succeeded / failed →（重试）/ dead |
| worker 池 | asyncio 协程池；按 provider 设并发上限（视频生成 ≤5 防限流，按注册表配置） |
| 重试 | 指数退避 + jitter，≤3 次；超限进死信（dead），CLI 可人工重放 |
| 优先级 | 交互式任务 > 批量任务；final 档渲染 > draft 档（数值越小越先调度） |
| 幂等 | 与 serving 层共享 idem_key：任务重放/进程重启不产生重复付费调用 |
| 恢复 | worker 崩溃后重启：running 态任务回滚为 pending 重跑（轮询型任务凭 task_id 续查，不重复提交） |
| 运维面 | `jobs list / stats / retry / cancel` CLI；队列深度纳入 stats 仪表盘与告警 |

**为什么 SQLite 而不是 Celery/Redis**：单机单用户、零外部依赖、任务与业务数据（shots/generations）同事务、断点语义简单；且 jobs 表本身就是数据链路的数据源。规模假设（并发 ≤5、日任务量 <1000）下 SQLite 队列绰绰有余——这是面试可辩护的"按规模选型"。

### 4.7 模型服务层（Model Serving）★ 新增（v1.1 网关的服务化升级）

**定位**：全部外部模型调用的唯一入口，以 OpenAI 兼容内部 API 形态提供服务。管线节点只认识 serving 层，不认识任何厂商 SDK。

表 4-2 模型服务层设计

| 机制 | 实现 |
|---|---|
| API 形态 | FastAPI 服务：`POST /v1/chat/completions`、`/v1/images/generations`、`/v1/videos/generations`（异步任务式）、`GET /v1/models` |
| 模型注册表 | YAML 声明每个模型：name/provider/capability/tier/单价/免费额度/并发上限/fallback_to/状态；热更新，改配置即上下架模型 |
| 路由 | task_type + tier → 候选模型链；成本感知（同能力选便宜）；**灰度路由：按百分比分流到新模型/新供应商**（如 seedance:kling = 90:10） |
| 失败降级 | 超时/失败指数退避 ≤3 次 → fallback 链切换（Seedance 2.0↔2.0-mini/fast 跨档位、DeepSeek↔Qwen 跨供应商；可灵接入后纳入视频 fallback）；provider 连续失败熔断（circuit breaker），半开探测恢复 |
| 幂等缓存 | idem_key = sha256(model + prompt + params + tier)，命中 generations 表 + 文件存在性校验直接返回 |
| 计费 | 每次调用记录模型/档位/用量/单价/成本；日预算硬熔断 ¥15/天 |
| 健康检查 | `GET /health` 汇总各 provider 状态；纳入 stats 仪表盘 |

服务化的实质收益（面试叙事）：① 编排层与厂商 SDK 解耦，换模型不改管线；② 注册表 + 灰度 = "模型的高效部署与服务化"的最小完整实现；③ 服务边界清晰后，serving 层未来可原样迁移到独立机器。

### 4.8 可观测性（Observability）★ 新增

表 4-3 可观测性设计

| 机制 | 实现 |
|---|---|
| 结构化日志 | 全系统 JSON lines 日志：ts/level/trace_id/node/job_id/model/cost/latency；trace_id = 一次项目运行 ID，贯穿编排节点 → 调度任务 → serving 调用 |
| metrics 采集 | 调用量、成功率、时延、成本、缓存命中率，按 model/tier/provider/day 维度落 SQLite |
| 仪表盘 | `onetake stats` CLI：今日/累计成本、成本燃烧速率、各环节成功率、模型对比表、队列深度、熔断状态 |
| 告警 | 预算燃烧超限、失败率超阈值、队列积压、provider 熔断 → CLI 显著警告 + 日志标记（不引入外部告警设施） |
| 成本报表 | v1.1 的 report 命令作为 stats 的一个视图保留（按项目/环节/档位明细） |

### 4.9 数据链路（Data Pipeline）★ 新增

**定位**：把系统运行中自然产生的数据（调用、质检、成片效果）沉淀为特征与报告，反哺决策——对应剪映 JD 的"数据处理、特征生产、风险感知与治理"。

表 4-4 数据链路设计

| 环节 | 实现 |
|---|---|
| 事件日志 | `events` 表（append-only）：generation / eval / publish / job 四类事件，全带 trace_id |
| 每日聚合 | 由任务调度器驱动的聚合任务：按日汇总 model_perf_daily（成功率/时延/成本/质量分）与 skill 效果（播放/完播回写） |
| 分析报告 | `onetake analyze`：模型质量-成本对比（Seedance vs 可灵）、skill 效果排行、失败原因分布 |
| 失败模式挖掘 | 聚合 VLM 质检不通过与生成失败记录，提炼规避性提示词 → 回写经验记忆（与记忆系统闭环） |
| 风险治理（轻量） | 内容审核结果落库（生成平台审核失败的记录）；Skill 配置敏感题材 blocklist（若未来发布，补 AIGC 声明检查清单） |

**数据驱动决策的硬指标**：至少完成 1 次基于运行数据的模型切换决策并记录（首个候选：P0 的 Seedance vs 可灵样片对比在运行数据上复验）。

### 4.10 Skills 技能库系统

与 v1.1 一致：YAML 定义创作方法论包（结构/风格/字幕样式/BGM/hooks），注册表管理，LLM 按选题选择；效果度量回写 metrics_json，由数据链路聚合支撑"Skill 优胜劣汰"。

### 4.11 长期记忆系统

与 v1.1 一致：画像记忆（确认节点修改意见 → LLM 归纳 → 人工确认写入）+ 经验记忆（质检不通过/失败后自动提取，v2.0 起由数据链路的失败模式挖掘增强）；Top-K 检索注入大纲/分镜 prompt；SQLite 存储。

### 4.12 质量评测（VLM-as-Judge）

与 v1.1 一致：每条镜头抽 3 帧 + 分镜描述送 `qwen3-vl-flash` 双维度评分（语义一致性/画面质量，各 1–5），任一 <3 自动重生成 ≤2 次（第二次附失败原因），仍不过标记人工介入；评测结果进事件日志与质量报表。

---

## 5 开发计划清单（快速迭代，9 周）

### 5.1 迭代总览

迭代三原则不变：① 每周五晚必须有可运行交付物；② 功能导向；③ 预算按阶段分配，超支即降级。

表 5-1 阶段总览

| 阶段 | 周次 | 功能目标 | 可运行交付物（周五验收） | 预算 |
|---|---|---|---|---|
| P0 | W1 | API 全打通 + 最小线性管线 | 固定文案 → 60s 无声草稿片的 CLI | ≤¥30 |
| P1 | W2 | 选题 → 脚本 + 分镜 + 分镜图 | 选题 → 分镜包（script.json + 图片） | ≤¥20 |
| P2 | W3 | 端到端 v1：选题 → 成片 | 一键产出带配音/字幕/BGM 的草稿成片 | ≤¥40 |
| P3 | W4 | Agent 化重构 + 成本工程 | 可中断续跑版 + 成本报表命令 | ≤¥20 |
| **P4** | **W5** | **模型服务化 + 任务调度** | **serving 服务上线 + 调度器承载视频生成** | ≈¥0 |
| **P5** | **W6** | **可观测性 + 数据链路** | **stats 仪表盘 + 每日聚合与对比报告** | ≈¥0 |
| P6 | W7 | Skills + 长期记忆 | 2 个 Skill 包 + 风格自动复用演示 | ≤¥30 |
| P7 | W8 | VLM 质检 + 成片闭环（不发布） | 质检回路 + 3 条成片产出（480p） | ≤¥40 |
| P8 | W9 | 开源与双 JD 简历交付 | 可复现 GitHub 仓库 + 演示视频 | ≈¥0 |

累计预算 ≤¥200，留 ¥100 机动。工时假设：工作日每晚 2–3 小时 + 周末每天 5–6 小时。

### 5.2 P0：奠基与最小管线（W1）

功能目标：所有外部依赖真实打通；最笨的线性代码证明"文案 → 视频"链路成立。

前置（开工核实已完成，结论见 3.x）：四个平台注册领额度；**换装带 libass 的 ffmpeg（本机 homebrew 构建无 libass，字幕烧录阻塞项）**；Pixabay 下载 5–10 首 BGM 建来源清单。

- 仓库骨架（pyproject / .env / gitignore）+ 五张核心表 + jobs/events 表（schema 一次到位）
- 网关骨架（本周为内部库形态：统一入口 + 计费日志 + 日熔断；服务化在 P4 做）
- 单点打通：DeepSeek JSON 输出 / Seedream 出图 / Seedance 5s 视频 / edge-tts 配音（带重试）/ FFmpeg 合成
- **Seedance 2.0 vs 2.0-mini/fast 各跑一条 5s 样片（同 prompt 同参考图），实测单价与质量对比，定草稿档主力并回填单条成本目标**
- 串联：`python main.py --script demo.txt` → draft.mp4

验收：一条命令产出 60s 草稿片；report 列出各环节成本；总花费 ≤¥30。红线：Seedance 2.0 两天调不通即切 2.0-mini/fast 或降 480p。

### 5.3 P1：脚本与分镜生成（W2）

与 v1.1 一致：大纲 Prompt（风格先写死知识科普）→ 分镜表 JSON Schema + 校验重试 → 角色设定表 + 参考图链 → 分镜图批量生成 + CLI 逐张确认（y/n/r）→ 台词时长对齐（改写 ≤2 次，仍超以音频为准）。

验收：3 个选题产出合法分镜包；分镜图人工一次通过率 ≥70%；单选题 ≤¥3。

### 5.4 P2：端到端 v1（W3）★ 项目最重要里程碑

与 v1.1 一致：视频批量任务管理（本周为简易队列，P4 迁移到调度器）、motion_prompt 自动生成、TTS 时长回写驱动 EDL、FFmpeg 合成（归一化/拼接/硬字幕/BGM 闪避）、`--topic` 全流程 + 两处人工确认、产出第 1 条自产视频。

验收：选题 → 60s 草稿成片一键产出，≤30 分钟、成本达标（P0 回填值）；声画同步无可见错位。

### 5.5 P3：Agent 化与成本工程（W4）

与 v1.1 一致：LangGraph 重构（StateGraph + TypedDict + checkpointer + 两处 interrupt，对照 P2 回归）；幂等缓存（idem_key + 命中率统计）；成本报表完整版；失败降级链演练。

验收：中途 kill 3 次恢复零重复扣费且产物一致；缓存命中率 ≥70%；report 输出单条成本报表。

### 5.6 P4：模型服务化 + 任务调度（W5）★ 新增（岗位 B 核心周）

功能目标：把"能跑的管线"升级为"平台化的系统"——模型调用走服务、异步负载走调度。

- 模型注册表 YAML Schema + 加载器（热更新）+ 全部已接模型录入
- FastAPI serving 服务：/v1/chat/completions、/v1/images/generations、/v1/videos/generations、/v1/models、/health
- 厂商适配器迁移到 serving 内部（DeepSeek/方舟/可灵/Qwen）
- 路由：tier 映射 + fallback 链 + 灰度百分比路由 + provider 熔断
- 管线节点全部改为 HTTP 调 serving（回归：同一选题产物与 P3 一致）
- 调度器：jobs 表状态机 + asyncio worker 池 + 按 provider 并发限流 + 指数退避重试 + 死信
- 视频生成任务从 P2 简易队列迁移到调度器；`jobs list/stats/retry` CLI

验收：kill worker 后恢复无任务丢失、无重复扣费；灰度演示（视频生成 10% 流量切 seedance 2.5）；管线节点 100% 经 serving 调用。

### 5.7 P5：可观测性 + 数据链路（W6）★ 新增（岗位 B 核心周）

功能目标：系统可观测、数据可沉淀、决策有依据。

- 结构化 JSON 日志 + trace_id 贯穿（编排节点 → 任务 → serving 调用）
- metrics 落库：调用量/成功率/时延/成本/缓存命中，按 model/tier/provider/day 维度
- `onetake stats` 仪表盘：今日/累计成本、燃烧速率、各环节成功率、模型对比、队列深度、熔断状态
- 告警：预算燃烧/失败率/队列积压/熔断 → CLI 警告
- events 事件日志接入（generation/eval/job 三类；publish 类随"不发布"决策取消，kind 枚举保留占位）
- 每日聚合任务（由调度器驱动）→ model_perf_daily / skill 效果表
- `onetake analyze`：模型质量-成本对比报告 + 失败原因分布；失败模式回写经验记忆
- 风险治理轻量版：审核失败落库 + Skill 敏感题材 blocklist

验收：单次运行 trace_id 可串全链路；stats 一屏看全系统状态；聚合报告产出；≥1 次数据驱动决策记录（复验 P0 的模型选择）。

### 5.8 P6：Skills 与记忆系统（W7）

与 v1.1 P4 一致：Skill YAML Schema + 加载器 + 注册表；实现知识科普解说、影视片段解说 2 个 Skill；LLM 选择器 + `--skill` 强制指定；记忆提取管线（确认意见 → LLM 归纳 → 人工确认写入）；Top-K 记忆注入；批量模式同 Skill 连出 3 条（**经任务调度器提交**）。

验收："第二次创作自动应用第一次风格偏好"录屏；批量 3 条 ≤¥20；Skill 切换风格差异肉眼可辨。

### 5.9 P7：质检与成片闭环（W8）

VLM 质检节点（抽 3 帧 → qwen3-vl-flash 双维度评分 → 低分重生成 ≤2 次）；质量报表；成片档渲染（**480p 即可**——2026-08-04 决策：不公开发布，开发者已有视频创作经验，无需以发布证明）；产出 ≥3 条成片留存作演示素材；质检数据回写数据链路（无 publish 事件，skill 效果以 VLM 质检分为代理指标）。

验收：质检真实拦截并自动重生成 ≥1 个镜头（留日志）；3 条成片产出（480p，本地留存可播放）；成片档单条成本达标；终稿一次通过率 ≥80%。

### 5.10 P8：开源与简历交付（W9）

- README：项目定位（双 JD 叙事）、五层架构图、demo 链接、成本数据、快速开始（30 分钟可复现验证）
- 3 分钟演示视频：选题 → 确认节点 → 成片 → stats 仪表盘 → 成本报表全流程录屏
- 技术复盘长文一篇：两档生成、幂等缓存、调度器/服务化取舍、数据驱动模型选型
- 双 JD 简历条目定稿 + 面试问答清单（附录 B/C）
- 代码清理：密钥外置、依赖锁定、issue 模板
- 回填"计划 vs 实际"成本到表 5-1

### 5.11 风险管理与最小可交付定义

表 5-2 风险登记册（2026-08 更新版）

| 风险 | 概率 | 应对 |
|---|---|---|
| API 定价/额度政策变动 | 中（已发生一次） | 注册表适配层隔离；pricing 随官方文档更新；本次核实记录即为应对样板 |
| 视频生成单价高于预期 | 已确认 | 草稿档 seedance 2.0-mini/fast 或 480p 档；数据链路持续对比，动态切换主力 |
| 视频供应商单点（可灵暂缓，仅方舟） | 低 | 方舟内多版本（2.0/mini/fast/2.5）互备；可灵已调研并预留适配器位，必要时走客服采购快速接入 |
| edge-tts 瞬断/失效 | 中（已实测瞬断） | 调用层重试 ≤3 次；持续失败切火山 TTS（¥1–5/万字符） |
| 本机 ffmpeg 无 libass | 已解决 | 已安装 `ffmpeg-full`（共存方案），项目内用 `/opt/homebrew/opt/ffmpeg-full/bin/` + `FFMPEG_PATH` 环境变量；开源复现另备 Martin Riedl 静态构建方案 |
| 视频生成质量不稳定 | 高 | 经验记忆沉淀规避提示词；选材避开人物大场景 |
| 时间不足 | 中 | 严格按里程碑砍 Scope：P5/P6 可压缩为演示级，P2 不可让渡 |
| 账单失控 | 低 | 日预算硬熔断 ¥15/天 + 平台侧消费告警 |
| 内容合规（未来若发布） | 低 | 本项目不公开发布；若改变主意：科普/解说正向题材、投稿必勾 AIGC 创作声明、BGM 换 Pixabay 曲目 |

最小可交付定义（底线方案）：P2 端到端 v1（必须保住）> P3 成本工程 > P4 服务化与调度 > P5 观测与数据 > P6 Skills > P7 质检成片。底线组合（端到端 v1 + 成本报表）仍覆盖岗位 A 职责 1/2/3/4，创作经验加分项由开发者既有经历覆盖；岗位 B 叙事最低限度依赖 P4（服务化+调度），故 P4 优先级高于 P5/P6。

---

## 6 附录

### 附录 A 推荐仓库目录结构（v2.0）

```
onetake/
├── README.md                  # 项目门面：架构图/demo/成本数据/快速开始
├── pyproject.toml
├── .env.example
├── main.py                    # CLI 入口：--topic/--script/--skill/--resume + report/stats/jobs/analyze
├── pipeline/
│   ├── graph.py               # LangGraph StateGraph（P3 起）
│   ├── linear.py              # 线性版管线（P0-2，保留作对照）
│   └── state.py               # TypedDict 状态定义
├── nodes/                     # 能力节点（每个对应一个图节点）
│   ├── outline.py  script.py  storyboard.py
│   ├── image.py    video.py   tts.py
│   ├── edl.py      render.py  demo_kit.py       # 演示物料（标题/封面，README 展示用）
│   └── judge.py               # VLM 质检
├── serving/                   # 模型服务层（P4 起；P0-3 为 gateway/ 内部库形态）
│   ├── app.py                 # FastAPI：/v1/chat/completions /v1/images /v1/videos /v1/models /health
│   ├── registry.py            # 模型注册表加载与热更新
│   ├── router.py              # 档位/成本/灰度路由 + fallback + 熔断
│   ├── idempotency.py         # sha256 幂等缓存
│   ├── pricing.py             # 单价表（2026-08-04 核实版，随官方更新）
│   └── adapters/              # deepseek.py ark.py kling.py qwen.py
├── scheduler/                 # 任务调度器（P4 起）
│   ├── queue.py               # jobs 表状态机
│   ├── worker.py              # asyncio worker 池 + provider 并发限流
│   └── retry.py               # 指数退避 + 死信
├── observability/
│   ├── logging.py             # 结构化 JSON 日志 + trace_id
│   ├── metrics.py             # metrics 采集落库
│   └── stats.py               # stats/analyze CLI 仪表盘与报告
├── datapipe/
│   ├── events.py              # 事件日志写入
│   └── aggregate.py           # 每日聚合 → model_perf_daily / skill 效果
├── memory/  store.py  extract.py  inject.py
├── skills/                    # Skill 包（YAML）
│   ├── knowledge_explainer_v1.yaml
│   └── movie_commentary_v1.yaml
├── editing/  edl.py  ffmpeg_builder.py  subtitle.py
├── db/      schema.sql  dao.py
├── assets/bgm/                # Pixabay 免版权 BGM + 来源清单
├── projects/                  # 运行产物（gitignore）
└── tests/                     # 节点级单测 + 端到端冒烟（mock serving）
```

### 附录 B 简历条目

**B.1 面向岗位 A（B 站 AI 创作）**

> OneTake（一条过）：端到端 AI 视频创作 Agent 工作台（独立开发，Python / LangGraph / FastAPI / FFmpeg，开源）
> - 对标 B 站 Updream 核心链路：构建"选题→脚本→分镜→素材生成→自动剪辑→成片"的 Agent 创作管线，支持节点级人工干预与断点续跑，端到端 30 分钟产出一条 60 秒视频；
> - 设计模型服务层统一接入 6 类生成模型：OpenAI 兼容内部 API + 成本感知路由 + sha256 幂等缓存（命中率 70%+）+ 全链路计费，单条成片成本压至 ¥X，全项目支出 <¥150；
> - 实现 Skills 技能库与长期记忆系统（Mem0 式提取-合并-注入），创作风格跨项目自动复用；
> - 引入 VLM-as-Judge 质检回路自动拦截低质镜头，终稿一次通过率 80%+；用自研工具产出 N 条成片完成端到端验证（本人另有 X 年视频创作经历，作品见……）；

**B.2 面向岗位 B（剪映 AI 架构）**

> OneTake：AI 视频创作 Agent 及其模型服务基础设施（独立开发，Python / FastAPI / LangGraph / SQLite，开源）
> - 设计并实现单机版模型服务平台：OpenAI 兼容 API + YAML 模型注册表 + 档位/成本/灰度路由 + 失败降级与熔断，承载 4 家供应商 6 类模型，管线节点 100% 经由服务层调用；
> - 实现 SQLite 任务调度器：状态机队列 + asyncio worker 池 + 按供应商限流 + 指数退避重试与死信重放，批量任务含重试成功率 95%+，worker 宕机恢复零任务丢失；
> - 建设可观测性与数据链路：结构化日志 + trace_id 全链路贯穿、metrics 仪表盘、事件日志 → 每日聚合 → 模型质量-成本对比报告，基于运行数据完成 1 次主力模型切换决策；
> - 上层为端到端 AI 视频创作 Agent（LangGraph 编排 + 人工确认节点），产出 N 条成片完成端到端验证。

### 附录 C 预判面试问题与答题骨架

表 6-1 高频问题（保留 v1.1 五条，新增基础设施向四条）

| 问题 | 答题骨架 |
|---|---|
| 为什么不用一键生成？ | 可控性与成本：分镜化把决策前置到便宜的图片阶段，"确认式自动化"（并举 Updream 画布理念对照） |
| Agent 系统和普通工作流有什么区别？ | 本项目演进即答案：线性脚本 → 状态图 + 质检回路 + 记忆注入（有感知、有决策、有学习） |
| 成本怎么控制的？ | 现场打开 stats：先图后视频、两档渲染、幂等缓存 70% 命中、便宜模型路由、日预算熔断 |
| 角色一致性怎么解决？ | 角色设定表（文本锚点）+ 参考图链（图像锚点）+ 人工兜底；坦承局限，IP-Adapter 为升级路径 |
| Updream 的下一个功能做什么？ | 素材语义搜索或 Skill 效果度量与推荐——均在 Could 档验证过可行性 |
| **调度器为什么用 SQLite 不用 Celery/Redis？** | 按规模选型：并发 ≤5、日任务 <1000；零外部依赖；任务与业务数据同事务；jobs 表即数据源。规模涨 10 倍再谈 Redis，讲清迁移路径 |
| **模型服务层解决了什么问题？** | 解耦（管线不识厂商 SDK）+ 治理（注册表/灰度/熔断）；现场演示 10% 流量切供应商、改 YAML 热更新上下架模型 |
| **怎么做模型选型的实验到上线？** | 完整走一遍：P0 样片对比 → 灰度 10% → 数据链路聚合报告 → 全量切换；强调"决策有数据记录" |
| **数据链路的数据从哪来、有什么用？** | 全部来自系统真实运行事件（零额外采集成本）；用途三选一举例：模型对比、失败模式回写记忆、skill 优胜劣汰 |

### 附录 D 文档使用说明

- 本文档为 v2.0 活文档，后续变更直接修改本文并在变更日志记录；v1.1 PDF 存档不再更新。
- 价格为 2026-08-04 实测核实数据，各平台以官方文档最新计费为准；`serving/pricing.py` 是唯一事实源。
- 每个 Phase 结束后回填实际成本与偏差到表 5-1，形成"计划 vs 实际"记录。

## 变更日志

- v2.0（2026-08-04）：新增目标岗位 B（剪映 AI 架构）及四大基础设施模块（任务调度/模型服务化/可观测性/数据链路）；回填 2026-08-04 价格与模型核实结论（Seedance 按 token 计费、DeepSeek-V4、百炼额度口径、可灵赠点证伪、edge-tts 瞬断、ffmpeg 无 libass、BGM/AIGC 合规）；工期 8→9 周；预算推荐档上调至 ≤¥150。
