# DEVLOG · OneTake 开发经验与踩坑记录

> 规则（见 AGENTS.md §6）：凡开发过程中遇到的非平凡问题——报错、环境坑、文档与现实不符、选型纠结——都记录在此。
> 每条按「现象 → 排查 → 根因 → 解决 → 经验」五段式，注明日期。这是技术复盘长文和面试问答的一手素材库。

## 2026-08-04 · 开工前核实阶段

### 001 本机 ffmpeg 烧不了字幕

- **现象**：`ffmpeg -vf "subtitles=t.srt"` 报 `Unknown filter 'subtitles'`
- **排查**：`ffmpeg -filters | grep subtitles` 为空 → `brew deps ffmpeg | grep libass` 为 0 → 确认 homebrew 官方把 ffmpeg 拆成 lite（默认）与 `ffmpeg-full` 两个 formula，lite 版无 libass/freetype
- **根因**：`subtitles`/`ass` 滤镜由 libass 库实现，lite 构建未编译该依赖
- **解决**：`brew install ffmpeg-full`（bottled 免编译），共存方案——项目内用 `/opt/homebrew/opt/ffmpeg-full/bin/ffmpeg`，代码走 `FFMPEG_PATH` 环境变量，全局 lite 版不动。中文字体用系统自带 Hiragino Sans GB，烧录实测通过
- **经验**：① "装过 ffmpeg" ≠ "ffmpeg 能用"，功能验证要落到具体 filter；② 副坑：ffmpeg-full 的依赖升级了 x265（.215→.216），导致 lite 版动态链接失效，`brew upgrade ffmpeg` 修复——共享库版本漂移是共存方案的固有成本；③ 开源复现需在 README 提供备选路径（Martin Riedl arm64 静态构建）

### 002 火山方舟 API Key 401 三层排查

- **现象**：方舟 API 调用全部 401 `AuthenticationError: the API key or AK/SK in the request is missing or invalid`
- **排查**：① 逐字节比对密钥字符串（`wc -c` 对比，一致）→ 排除复制错误；② 确认端点与鉴权方式（`Bearer` + `ark.cn-beijing.volces.com/api/v3`，与官方一致）→ 排除调用方式；③ 测试 ID / Secret / ID.Secret 三种 Bearer 组合均 401；④ 检查 key 的「修改权限」，发现权限范围只有「智能处理」「Viking AI 搜索」两个与方舟无关的 scope → 定位到 **key 建错了页面**
- **根因**：key 在「火山引擎统一密钥管理页」创建（那里只有其他产品线的 scope），而方舟专属 API Key **只能在方舟控制台内**（console.volcengine.com/ark → API Key 管理）创建。正确 key 为 `ark-` 前缀格式，错误 key 为点号分隔长串
- **解决**：方舟控制台内重建 key，验证通过
- **经验**：① **401 三层排查法**：凭证字符串 → 凭证类型/鉴权方式 → 凭证权限范围，由表及里各有验证手段；② 云平台两级凭证体系（IAM AK/SK 签名鉴权 vs 产品级 API Key Bearer 鉴权）不可混用；③ 厂商会把权限不足报成 401 而非 403（安全混淆），排查时别被状态码字面意思带偏；④ 网关适配层设计启示：把厂商混合错误码翻译成内部统一错误类型，避免上层误判重试

### 003 edge-tts 瞬断

- **现象**：首次调用 edge-tts 报 `NoAudioReceived`，产出 0 字节文件；重试即成功
- **根因**：edge-tts 是非官方接口（走微软 Edge 大声朗读服务），存在瞬断/限流
- **解决**：调用层封装 ≤3 次重试；备选火山 TTS（¥1–5/万字符，一条 60s 配音约 ¥0.1–0.5）
- **经验**：免费/非官方依赖必须假设其不可靠，重试与降级预案在第一次接入时就写好，而不是挂了再补

### 004 文档滞后：PRD 模型 ID 与 draft 策略现形记

- **现象**：PRD 写的 `doubao-seedance-1-5-pro-250428` 在真实账号的 `GET /models` 中不存在；实际 ID 为 `-251215` 且状态 **Retiring**；同时发现 Seedance 2.0 已改按 token 计费（单价约为 PRD 口径 6–8 倍）、2.5（`doubao-seedance-2-5-260628`）已上线
- **解决**：以真实账号模型清单为准更新 pricing 与策略；草稿档改为 P0 实测对比可灵 2.5 Turbo / seedance 2.0 mini/fast 后定
- **经验**：所有模型选型结论必须用真实账号验证，文档和第三方文章永远滞后——"P0 实测回填"机制就是这个思想的制度化；`GET /models` 是零成本的选型核实手段，接入新平台第一天就该跑

### 005 开工前价格核实的方法论

- **做法**：动手前花半天联网核实四家平台的最新模型 ID、价格、免费额度，结果证伪/修正了 PRD 的四处假设（Seedance 计费方式、DeepSeek 模型名、百炼额度口径、可灵赠点）
- **经验**：PRD 里的价格表本质是"写作时刻的快照"，必须标注数据时点并留回填机制；核实时优先官方价格页，第三方整理只作线索——第三方普遍滞后且互相抄

### 006 可灵 API 采购门槛 → 供应商策略调整

- **现象**：注册可灵开放平台后发现 API 不支持在线充值，控制台提示"如需购买，请联系客服团队"——只适合走商务采购的大客户，不适合 ¥300 小额个人项目
- **决策**：暂缓接入。降级链与灰度演示改用方舟内多版本互备（seedance 2.0 ↔ 2.0-mini/fast ↔ 2.5）；跨供应商机制演示由 DeepSeek ↔ Qwen 承担（LLM 侧不受影响）；可灵作为"已调研待接入"供应商保留适配器位（其 JWT 鉴权差异已在设计中预留）
- **经验**：① 供应商调研的评估维度不只是价格和能力，**获取门槛**（采购流程、最小充值、实名要求）同样决定可用性；② 多供应商架构的价值不在于真的接入 N 家，而在于任何一家都可插拔——设计不锁定，可灵随时能插回来；③ 坦承"视频供应商单点"风险并写入风险表（方舟多版本互备 + 必要时走客服采购），比假装有多供应商更专业

### 007 范围决策：不公开发布作品

- **背景**：原 PRD 要求产出 ≥3 条视频发布 B 站，作为岗位 A"视频平台创作经验"加分项的证据。开发者本身已有丰富视频创作经历，该加分项无需本项目证明
- **决策**：砍掉公开发布环节。连带调整：成片画质标准 720p → 480p（成片预算 ¥60–90 → ¥20–40）；BGM 版权约束放宽（Pixabay 仍为首选，因 README 演示录屏属公开传播）；P7 改为"质检与成片闭环"；skill 效果度量失去播放/完播数据源，改用 VLM 质检分为代理指标；最小可交付顺序改为 P2 > P3 > P4 > P5 > P6 > P7
- **经验**：① 简历项目的范围应围绕"还差什么证据"来砍——已有的证据（创作经历）不重复建设；② 砍范围时要做全文档一致性扫描（本次同步了 prd/TODO/AGENTS 三个文件十余处引用），留一处矛盾就是面试时被追问的坑；③ 法规类备查项（AIGC 声明）不删除而是标注触发条件，未来改主意时不用重新调研

## 2026-08-05 · P0 奠基阶段

### 008 Seedance 全家族 ModelNotOpen——「模型在清单里」≠「账号已开通」

- **现象**：Seedance 2.0（`doubao-seedance-2-0-260128`）创建任务即 404 `ModelNotOpen: Your account has not activated the model`；逐一探测 2.0 / 2.0-fast / 2.0-mini / 2.5 / 1.0-pro 共 9 个视频模型 ID，全部未开通或不存在。同账号 Seedream 文生图却一切正常
- **排查**：开工前核实用 `GET /models` 确认了模型 ID 存在——但那只是平台**目录**；方舟的模型是「开通制」，要在控制台对每个模型单独点「开通」才能调。错误码区分得很清楚：`ModelNotOpen`（模型存在但没开通）vs `InvalidEndpointOrModel.NotFound`（ID 本身不存在）
- **根因**：TODO 0.2 的「开通 Seedance 模型服务」待办未完成（控制台人工动作，还关联实名认证/充值），API 侧无开通接口
- **解决**：TODO 0.5 Seedance 项与 0.6 对比项标记 [!] 阻塞；P0 草稿片改为全分镜图 Ken Burns 推近 + TTS + 硬字幕合成（`--video-shots 0`），管线留好视频镜头接口，开通后 `--video-shots N` 即恢复
- **经验**：① 平台核实要分三层——模型存在（目录）→ 账号已开通（服务）→ 账号有余额（计费），每层独立验证，`GET /models` 只覆盖第一层；② 探测未开通模型零成本（create 秒级 404 不计费），适合全家族批量探测；③ 管线「图片填充 + 视频点缀」的分层设计让视频供应商阻塞不影响主交付物——分层降级不只省钱，也是可用性兜底

### 009 uv 装依赖 PyPI 超时

- **现象**：`uv add` 从默认 PyPI 源拉包超时（`files.pythonhosted.org` connect timeout）
- **解决**：`UV_DEFAULT_INDEX=https://pypi.tuna.tsinghua.edu.cn/simple uv add ...` 秒装
- **经验**：国内网络先配镜像再装包；uv 会把索引写进 pyproject.toml（`[[tool.uv.index]]`），对开源复现者友好

### 010 Seedance 开通需 ¥200 底额 → 视频模型推迟至 P2

- **现象**：准备开通 Seedance 时发现视频模型开通要求账号充值 ¥200 底额（图模型 Seedream 无此门槛，所以 P0 出图一路畅通）
- **决策**：推迟到 P2（W3）开工前再充值开通。理由：P1（脚本/分镜/分镜图）完全不需要视频模型；`--video-shots 0` 全图卡模式已能支撑管线调试；¥200 底额在 ¥300 总预算中占比 2/3，推迟充值可让资金占用与实际需求对齐
- **连带调整**：`main.py run --video-shots` 默认值 1 → 0（全图卡为默认路径）；0.5 Seedance 单点与 0.6 供应商对比标记延期，移入 P2 开工前提；prd.md 3.5 补充底额说明
- **经验**：① 平台的"开通门槛"（底额/实名/企业认证）和"使用价格"是两件事，开工前核实覆盖了价格但没覆盖开通门槛——供应商调研清单再加一个维度（至此已有：价格、能力、获取门槛、开通门槛）；② 管线设计上的「真实视频镜头数可开关」（`--video-shots N`）再次证明其价值：视频模型缺席时主交付物不受影响，这也是"先图后视频"架构的韧性红利；③ 现金流也是成本工程：同样的总预算，支付时点越贴近使用时点越好

### 011 P1 小结：台词时长的系统性偏差与"校准前置"

- **现象**：做梦选题 9 个分镜有 8 个首轮台词偏短触发改写；蜂巢选题只有 1 个触发。同一个分镜 prompt，首轮合格率波动大
- **分析**：不是随机噪声——分镜 prompt 要求"15–40 字台词配 4–8s 镜头"，但中文语速约 4 字/秒，7s 镜头实际需要 ~28 字，LLM 倾向写偏短台词。**prompt 只给了两端约束，没给换算关系**，模型只能猜
- **对策（留 P2 或 prompt 迭代时做）**：把语速换算直接写进分镜 prompt（"7 秒镜头约需 28 字"），把校准从"生成后改写"前置到"生成时就对"——首轮合格率应作为 prompt 效果的度量指标进 P5 数据链路
- **经验**：① "生成 → 校验 → 改写"的兜底链能兜住质量，但每一轮改写都是钱和时间——兜底机制的存在不妨碍追求"一次就对"；② 系统性偏差 vs 随机噪声的辨别方法：多个样本的偏差方向是否一致（全部偏短 = 系统性 = 改 prompt；有偏长有偏短 = 噪声 = 靠兜底）；③ 这类发现只有"每个分镜都有实测数据"才看得见——时长回写机制顺手成了数据采集器

### 012 Seedance 三档实测对比：token 数与档位无关，单价决定一切

- **实验**：同 prompt、同 5s 480p，Seedance 2.0 标准 / 2.0-fast / 2.0-mini 各一条样片
- **数据**：三家 completion_tokens 完全相同（50638）；成本 2.31 / 0.71 / 待核实 元；耗时 150.7 / 93.2 / 83.4 s；抽帧目检画质均合格
- **结论**：token 数 = 分辨率 × 时长 × 帧率的函数，与模型档位无关；**选档本质是在选每 token 单价，不是在选"用量"**。草稿档定 2.0-fast（14 元/M，比标准档省 69%）
- **经验**：① 按 token 计费的视频模型，成本优化的空间不在"省 token"（那是分辨率和时长决定的），而在"选单价"和"减少无效生成"（先图后视频、幂等缓存）——这精确解释了本项目成本策略为什么长这样；② 计费按 usage 实测反算优于按官方每秒参考价估算（pricing.py 已改为 token 优先）；③ 占位价进库要标注"待核实"并可回溯修正（本次 mini 行置零标注，避免污染报表）——**成本报表的可信度来自每一条记录都可审计**

### 013 图生视频首帧锚定：base64 内联可行

- **假设**：火山视频 API 接受 base64 data URL 作为首帧（本地分镜图 → 图生视频，无需公网 URL/对象存储）
- **探针**：蜂巢 shot_01.png（486KB → base64 648KB）作首帧 + 运动指令，2.0-fast 生成 5s 视频（¥0.71，85.6s）
- **验证**：API 成功 + **抽首帧与原图肉眼比对，构图/角色完全一致**——锚定真实生效（防"API 不报错但首帧被忽略"的阴故障）
- **结论**：图生视频路线成立，零额外依赖（不上 TOS）。批量生成按此路线：分镜图 base64 首帧 + motion_prompt
- **经验**：探针验证要防"假阳性"——API 返回成功不等于语义生效，涉及"参考/锚定"类参数时必须用产物比对确认

### 014 熔断真实触发 + 一个"假幂等" bug

- **事件**：彩虹选题冒烟跑到第 10 镜时，日预算熔断首次真实触发（当日 ¥15.69 ≥ ¥15），shot 10 三次重试被拦、标记失败、其余 9 镜正常——熔断 + 失败隔离 + 重试三机制在真实失败中协同，P3 的演练计划被真实环境提前完成
- **bug**：中止提示写"重跑同命令即可（幂等续跑）"，但 `run --topic` 每次都新建项目——重跑是全量重做（多花 ¥8.5），提示语承诺了代码做不到的事。用户要求实测幂等时暴露
- **修复**：`run_topic` 增加 `--pid` 续跑模式。续跑验证：图跳 10 张 ¥0、对齐 ¥0、只补 shot 10 ¥0.71、增量成本精确等于 ¥0.71——"断点续跑零重复扣费"从口号变成可验证事实
- **经验**：① 熔断类护栏要挑真实场景验收（这次被动验收胜过任何设计评审）；② **提示语/文档也是契约**——写了"幂等续跑"就必须兑现，写之前先想代码能不能做到；③ 幂等要区分层级：单阶段幂等（文件跳过）≠ 编排幂等（项目续跑），后者需要显式的 resume 入口（--pid）

### 015 amix 默认增益均分：人声被"合法地"减半

- **现象**：用户试听成片反馈听不到 TTS。测量定位：分段/拼接/成片三级音频流都在（排除丢轨），但成片前 6.4s 的 RMS 只有纯 TTS 的 52%
- **根因**：FFmpeg `amix` 滤镜默认 `normalize=1`——按输入路数均分增益（2 路 ×0.5），人声被合法地砍半，再和 BGM 竞争就不明显了
- **修复**：`amix=normalize=0`，人声恢复满幅（修后 RMS 2037 ≈ 纯 TTS 1951 + BGM 底）。重新渲染零 API 成本（幂等的本地重渲染正是 EDL 分离的红利）
- **经验**：① "文件有音轨" ≠ "听得清"——音频问题的排查要落到**响度测量**（volumedetect / RMS），不能只看流存在；② 滤镜的默认值也是行为，混音/压缩类滤镜的默认参数多半不符合你的场景，用之前查文档（amix 的 normalize、sidechaincompress 的 makeup 都是坑点）；③ 用户试听是最便宜的 QA——这条 bug 机器验收（流存在、时长对）全绿，人耳一秒抓出来

### 016 角色锚点无条件注入：吉祥物增强 vs 叙事主角灾难

- **现象**：用户手动跑"织田信长的一生"，发现每张图都趋同、都有同一个人物剪影——包括市集、战场等本不该有人物的场景镜
- **根因**：`create_images` 无条件把 character_sheet 拼进每个 visual_prompt；该选题的 sheet 含"信长以侧面或背影出现、反复出现"，模型忠实执行 → 场景镜也被强制加入人物。参考图链（首图恰好是人物特写）进一步放大趋同
- **为什么前三个选题没事**：吉祥物型锚点（月牙/蜜蜂/水分子）本就适合每镜出现；叙事型主角（信长）只该在关键镜头出现——**同一机制，对吉祥物是增强，对叙事主角是灾难**
- **修复方向（P3+ backlog）**：① 分镜加 `has_character` 标记，角色锚条件注入；② sheet 拆「角色锚/风格锚」两段，风格锚（色调/画风/意象）恒注入；③ 参考图链分级（人物镜链人物首图，场景镜不用参考图）
- **经验**：① "零成本手段"的代价是粗粒度——文本锚点控制不了"出不出场"这种语义级开关，只能控制"长什么样"；② prompt 拼接类机制要对**每个选题类型**验证，不能只在有利样本上验收；③ 分镜图确认点（y/n/r）正是为拦截这类系统性偏差设计的——跳过确认的同时，也跳过了发现问题的机会

### 017 无声疑云：Seedance 内嵌音轨 + 两次验收方法误判

- **bug 本体**：Seedance 2.0-fast 生成的视频自带 AAC 音轨（AI 自动配乐/音效）。分段合成 `-i 视频 -i TTS.mp3` 未写 `-map`，ffmpeg 默认选流丢弃了 TTS——用户"没有声音"的反馈完全正确
- **修复**：分段加 `-map 0:v -map 1:a`（画面取视频、声音取 TTS，丢弃内嵌音轨）
- **误判一（DEVLOG 015 修正）**：此前 amix normalize=0 的"修复验证"是 RMS 巧合——被丢弃的 TTS 和顶包的 Seedance 音效响度同量级，当时人声根本不在
- **误判二**：`-map` 修复后零滞后相关性显示"仍无声音"（0.046），实为 AAC priming（1024 样本 ≈23ms）偏移导致相位错位；滞后补偿扫描证实人声在（0.589 @ 23ms）
- **遗留**：concat 重封装丢 edit list → 23ms 音画偏移（<45ms 感知阈值，入 backlog）
- **经验**：① 用户反馈永远先信，测量方法错了两次、用户的耳朵对了三次；② 验证音频内容一致性：响度（标量会巧合）< 零滞后相关（对编码延迟过敏）< **滞后补偿波形相关**；③ 媒体处理的隐性知识（priming/edit list/默认选流）是"懂行"与"会调参"的分水岭

### 018 缓存上线：两个只出现在"第二条路径"的 bug

- **背景**：3.1 幂等缓存（内容寻址，sha256 收敛在网关）上线，全链路命中验证通过（同 pid 重跑增量 ¥0、12 秒、命中率可统计）
- **bug 1（返回结构契约）**：缓存命中返回的 dict 少了 `latency_ms` 字段，调用方 `r["latency_ms"]` KeyError——真实路径和缓存路径是**两个返回结构**，只测了真值路径。修复：缓存返回补 `latency_ms: 0`
- **bug 2（指标归属）**：cache_hit 记录 project_id 写成 None，按项目统计命中率显示 0%——功能对、指标错。修复：命中留痕带调用方 project_id
- **经验**：① 缓存引入的是**并行代码路径**——所有调用方契约（返回字段、异常语义）必须在两条路径上保持一致，验收要两条路径各测一遍；② "功能正确但统计错误"是最隐蔽的指标 bug——数据写入时就丢了归属维度，事后无法修补（首批 24 次命中永远丢失归属）；③ 把失败也变成数据：两次验证失败都没花钱（KeyError 拦在付费前），缓存系统的自指验证近乎零成本

### 019 LangGraph 上线：完成态空转语义 + 恢复粒度与缓存迁移成本

- **落地**：`pipeline/graph.py` 七节点链 + 两 interrupt 确认点 + SqliteSaver（checkpoints.db）；节点为能力函数薄包装，能力层零改动；线性版保留对照（`--graph` 双轨）
- **语义发现 1**：同 thread_id 跑到 END 后再 invoke 是**空操作**（返回旧最终状态）——曾误判为"渲染 bug"（rm 产物后重跑未重建，实为图未执行）。CLI 需区分"崩溃恢复"（同 pid）与"整片重做"（新 pid/清 checkpoint）
- **语义发现 2**：kill -9 演练证实恢复以**节点为粒度**——崩溃时正在执行的节点从头重跑，但该节点内已完成的调用被幂等缓存接住（崩溃前生成的 shot_01/02 图在恢复时零成本跳过）
- **迁移成本**：老项目（无 idem_key 时代产物）首次走新缓存会全量 miss 重新生成（彩虹视频 ¥7.1）——一次性；此后全部命中。指标口径上应注明"缓存覆盖自 3.1 上线起"
- **经验**：① 框架语义要用实验确认（"恢复"到底恢复什么）——invoke(None) 只对未完成图有效这条，文档不细读不会知道；② 恢复演练的验收要拆成"位置正确"与"成本正确"两项分别核：这次位置全对、成本因老资产无指纹而有损——分开报告才准确；③ 熔断的"调用前检查"语义意味着跨线那次调用会完成——日预算是近似护栏，精确到次会误伤可用性

### 020 降级链演练：主备切换透明生效 + 一个厂商怪癖

- **落地**：网关 FALLBACKS 表（DeepSeek→Qwen 跨供应商、Seedream 4.0→4.5 跨版本、Seedance 2.0↔fast 跨档）；主调用失败 → 记 failed → 切备胎重试一次 → 返回带 `degraded_from`；**降级结果不写 idem_key**（防降级污染缓存，下次仍优先主力）
- **演练 1（LLM）**：模拟 DeepSeek 断连 → Qwen 自动接管，返回合法 JSON，¥0.0001，对调用方透明
- **演练 2（视频）**：标准档注入故障 → fast 接管，¥0.71，degraded_from 留痕可查
- **意外收获**：首次 drill 撞出 Qwen 怪癖——json_object 模式要求 prompt 必须含 "json" 字样（生产 prompt 都有"输出严格 JSON"所以没事，演练 prompt 没有才暴露）。**降级路径必须演练**：两条供应商路径看似等价，怪癖只在真实切换时暴露
- **经验**：① 降级的正确姿势是"对业务透明 + 对账本诚实"——调用方无感知，但 degraded_from 必须落库可查；② 备胎产物不进缓存——降级是临时状态，不能让临时产物获得长期缓存身份；③ 熔断语义边界：日预算是"调用前检查"的近似护栏，跨线那次调用会完成（今日 ¥16.38 > ¥15 即此），精确护栏需要预授权机制，单机项目不值得

### 021 灰度路由 × 缓存的首次交火 + key 污染雪崩

- **事件 A（key 污染雪崩）**：serving 首跑时 pydantic 自动填默认值（size）+ tier 并入 payload → 同内容经 HTTP 与直连产生不同 idem_key → 旧缓存静默 miss；更糟的是污染运行覆盖了产物文件 → 参考图内容变化 → 下一轮连锁 miss。修复：指纹原料只含语义参数（剔除 out_path/tier/model，pydantic 默认 None 不填充）
- **事件 B（灰度×缓存）**：终验跑非零成本（¥4.66）——排查发现 8 个视频调用中 2 个被权重路由分到 10% 的标准档（2×¥2.33），模型不同 → key 不同 → 真实生成。**这不是 bug，是灰度在真实工作**：缓存按模型分别记忆，灰度采样到的候选模型首次调用必然 miss，之后命中
- **经验**：① 内容寻址缓存的不变量——指纹原料必须只含语义参数，传输层的任何归一化（默认值填充/路由字段并入）都必须先剥离；② key 污染的后果会雪崩（覆盖产物 → 级联 miss），修 key 只是止血，还要理解产物文件已被改写；③ **灰度路由与缓存在语义上天然张力**：灰度要"真实采样候选"（故意不确定），缓存要"同内容同结果"（刻意确定）——共存方式是指纹含模型维度，每个候选的结果各自缓存；④ 演示"全命中 ¥0"前要先想清楚系统里有哪些**故意的随机源**（灰度权重就是一个）
